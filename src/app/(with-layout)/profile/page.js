export const metadata = {
    title: 'Profile | Next Hero',
    description: 'Next Hero',
}

const ProfilePage = () => {
    return (
        <div>
            Profile Page
        </div>
    );
};

export default ProfilePage;